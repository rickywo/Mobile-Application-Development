<resources>
    <color name="riki_bg_color">#0C101E</color>
    <color name="riki_text_color">#FFFFFF</color>
    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="android:background">@color/riki_bg_color</item>
        <item name="android:textColor">@color/riki_text_color</item>
    </style>

</resources>
